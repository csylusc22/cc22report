# 作品集网站文字与排版修改指南

## 1. 应该编辑哪些文件

网站源文件都在当前文件夹中：

- `index.html`：修改所有页面上的文字、标题和图片路径。
- `styles.css`：修改文字大小、颜色、位置、间距和图片尺寸。
- `app.js`：控制文字树、文字飘落、菜单和滚动等交互。只改文字排版时不需要动它。
- `assets/`：存放网站使用的图片和视频。

建议使用 Visual Studio Code。也可以右键文件，选择“打开方式 → 记事本”，但 VS Code 更容易查找和撤销。

修改之前，先复制整个网站文件夹或保留一份 ZIP 备份。

## 2. 修改首页文字

打开 `index.html`，首页内容位于约第 74—108 行。直接修改标签中间的文字，不要删除 `<`、`>` 以及标签名称。

例如：

```html
<p>LIFE CONTINUE TO GROW</p>
<h1>生命持续生长</h1>
```

只修改为：

```html
<p>YOUR NEW ENGLISH TITLE</p>
<h1>你的新中文标题</h1>
```

首页各区域对应关系：

| 区域 | HTML 类名 |
| --- | --- |
| 日期和“作品补充” | `cover-date` |
| 中英文主标题 | `cover-title` |
| 圆圈内“生长” | `growth-seal` |
| “保持纯粹……” | `cover-motto` |
| “叙事空间＋新探索” | `cover-theme` |
| 姓名与专业 | `cover-sign` |

## 3. 修改首页元素位置

打开 `styles.css`，首页桌面端位置主要位于约第 79—110 行。

位置通常由下面四个属性控制：

```css
.cover-title {
  left: 8vw;
  top: 45%;
}
```

- `left`：距离页面左边的位置。数值越大，元素越向右。
- `right`：距离页面右边的位置。数值越大，元素越向左。
- `top`：距离页面顶部的位置。数值越大，元素越向下。
- `bottom`：距离页面底部的位置。数值越大，元素越向上。

例如，把主标题向右移动、向下移动：

```css
.cover-title {
  left: 12vw;
  top: 52%;
}
```

不要在同一个元素里同时使用 `left` 和 `right`，也不要同时使用 `top` 和 `bottom`，除非你清楚它们的共同作用。

## 4. 修改字号、颜色和字距

主标题当前样式：

```css
.cover-title h1 {
  font-size: clamp(36px, 4.6vw, 72px);
  letter-spacing: .07em;
  color: #4c4942;
}
```

- `font-size`：字号。
- `letter-spacing`：字距。数值越大，文字越疏。
- `color`：颜色，可以填写十六进制色值。
- `line-height`：行距。
- `font-family`：字体。

`clamp(36px, 4.6vw, 72px)` 表示字号会随屏幕变化，但不会小于 36px，也不会大于 72px。如果希望固定字号，可改为 `font-size: 52px;`。

## 5. 修改首页整张底图

桌面端对应样式：

```css
.cover-background {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center center;
}
```

- `object-fit: cover` 让图片铺满首页。
- `object-position` 控制底图显示重点，例如改成 `60% center` 会让画面更偏右。
- 不建议修改图片文件名，否则页面会找不到素材。

当前整张底图是 `assets/cover-background-user.png`，树枝、飞鸟、灰色方块和四只栖鸟都已经合并在这张图中。红框印章已从底图移除。

## 6. 修改首页字体和生长动效

首页会优先使用电脑中安装的“可画周易清刻本-简”。如果没有安装，会自动使用网站内嵌的 `assets/fonts/cover-qingke-fallback.ttf`，因此拷贝网站时请保留整个 `assets` 文件夹。

文字树与飘落动效在 `app.js` 的 `Interactive typographic tree` 段落：

- `branches`：每一条枝桠的路径和位置。
- `birth`：文字出现时间，数值越大出现越晚。
- `release`：文字开始掉落的时间范围。
- `floor`：文字落到页面底部后停住的位置。
- `.032`：掉落重力，数值越大下落越快。

不熟悉 JavaScript 时，建议只调整 CSS 位置，不要改动这段逻辑。

## 7. 手机端需要单独调整

`styles.css` 约第 284 行之后是平板和手机样式：

```css
@media (max-width: 900px) { ... }
@media (max-width: 560px) { ... }
```

如果你只修改前面的桌面样式，手机端可能仍会使用这里的覆盖数值。因此大幅移动元素后，也要检查并修改这两个区域内同名的类。

## 8. 修改个人介绍页与信封卡片

个人介绍页位于 `index.html` 中的 `<section id="about">`：

- `profile-text-card`：个人简介文字。
- `philosophy-text-card`：设计理念文字。
- `software-skills`：软件图标与名称列表。
- `profile-photo-deck`：从蓝色文件夹后方升起的三张个人照片。

三张个人图片是从提供的 SVG 原稿中提取的：

- `assets/about-personal-1.webp`
- `assets/about-personal-2.webp`
- `assets/about-personal-3.webp`

新增技能时，复制一个 `software-skills` 里的 `<li>`，修改 `--skill` 色值、`skill-icon` 内的缩写和 `<strong>` 内的软件名称。

文件夹大小、照片位置、内容窗和技能矩阵主要在 `styles.css` 的 `Supplied personal-profile folder framework` 段落修改。文件夹点击开合逻辑位于 `app.js` 的 `profileFolder` 段落。

## 9. 保存和查看结果

1. 在编辑器中按 `Ctrl + S` 保存。
2. 返回浏览器，按 `Ctrl + R` 刷新。
3. 如果仍显示旧内容，按 `Ctrl + F5` 强制刷新。
4. 改坏时按 `Ctrl + Z` 撤销，或从备份 ZIP 重新解压。

网站可以直接双击 `index.html` 打开。请始终完整保留 `index.html`、`styles.css`、`app.js` 和 `assets/` 文件夹。
